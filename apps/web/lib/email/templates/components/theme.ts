/**
 * The email design tokens live in `@louez/email` so the platform emails and
 * the store-branded ones share one shell. Re-exported here for the templates.
 */
export { DEFAULT_PRIMARY_COLOR, emailTheme, shell, styles } from "@louez/email/templates";
